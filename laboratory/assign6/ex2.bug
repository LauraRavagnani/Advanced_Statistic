model{
    # likelihood
    for(i in 1:Ntot){
        tested[i] ~ dbern(theta[patient[i]])
    }

    # Beta(3, 100) prior for vaccinated and placebo
    for(k in 1:2){
        theta[k] ~ dbeta(3, 100)
    }
}

